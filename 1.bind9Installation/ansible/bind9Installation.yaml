---
- name: Install and configure Bind9 DNS server
  hosts: dnsServers
  become: true
  tasks:
    - name: Check health status of DNS server
      ansible.builtin.ping:

    - name: Update the server's repository index
      apt:
        update_cache: yes

    - name: Install Bind9
      apt:
        name: bind9
        state: latest